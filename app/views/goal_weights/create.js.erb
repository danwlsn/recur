$('.gw p').html("<%= current_user.goal_weights.last[:weight] %> <%= current_user.option[:weight] %>");
$('.general--input').val("");
$('.flash').html("<%= escape_javascript(render :partial => 'layouts/flash', :locals => { :flash => flash}).html_safe %>");
